#include<stdio.h>

int main()
{
    float width, height, area;
    printf("Enter Width: ");
    scanf("%f", &width);

     printf("Enter Height: ");
    scanf("%f", &height);

    printf("The Area of Recangular is: %f", area=width*height);
    return 0;
}
